# Diff hygiene

Keep changes ticket-scoped. Do not mix formatting-only rewrites with feature changes unless the ticket is explicitly for formatting. Avoid committing generated caches or `.agent-local/`.
