# Code style

- Main React component files use `export default function ComponentName() {}` when they represent the file.
- Utilities and multi-export files use named exports.
- Component files use PascalCase. Utility/API files use lowerCamelCase.
- Keep import order: framework, external packages, project packages, app aliases, relative imports.
