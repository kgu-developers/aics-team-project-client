# Accessibility

Prefer semantic tags such as `header`, `nav`, `main`, `section`, and `article`. Add labels or `aria-label` for interactive controls. Keep Loading, Error, and Empty states visible and understandable.
