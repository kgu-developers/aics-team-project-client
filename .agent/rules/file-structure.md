# File structure

Keep OOP-specific code under `apps/oop/src/course`. Move code to `packages/*` only when it is likely to be reused by another course app. Do not bootstrap `apps/database` without an actual ticket.
