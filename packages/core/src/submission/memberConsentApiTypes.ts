/** Current-version summary. The server does not return a version or member list. */
export type StudentSubmissionMemberConsent = {
  confirmedCount: number;
  totalCount: number;
  isConfirmedByMe: boolean;
};
